# pnpm configuration for the WiseEvidence workspace.
# Peers are auto-installed so the Astro + React island stack resolves cleanly.
auto-install-peers=true
# Keep the store deterministic; CI installs with --frozen-lockfile.
strict-peer-dependencies=false
